# 交接说明（2026-09-16）：PR #12 同步与 H3 预检界面

## 当前代码

- 用户已手动合并 [PR #12](https://github.com/ND5-0348/erp-ledger-system/pull/12)。原工作目录 `E:\project\erp-ledger-system-main` 已快进到 `cfa9b35dd729e505c4c9cbc49443110227fd36af`，本地 `main` 与 `origin/main` 一致。
- 本轮 H3 新代码及原有财务公式修复已整合在上述原目录，尚未提交或推送。本轮没有操作服务器、导入真实业务数据或迁移本地业务库。
- 同步前的 14 个已修改文件及 6 个未跟踪代码文件已保存并恢复；备份 stash `a10866eb3cf65b33a982a9bd3f2bb91fefb06793` 仍保留。四处冲突已按四级结构、写入锁及财务公式语义合并，不要再次应用该 stash。
- `.temp/pr12-h3` 是合并前建立的临时工作树，代码不是最终版本。该目录内的 Python 3.12 虚拟环境用于本轮测试。`outputs/h3-preview-20260916/h3-changes.patch` 也是早期转移补丁，不含最后整合修复，不作为最终交付补丁。

## 本轮完成

1. 基本信息页面增加按导入权限显示的“旧台账预检”入口。预检支持上传、会话恢复、服务端分页，展示原值、当前值、历史顺序、每期日期金额票据、来源列和阻断原因。
2. 人工修正采用结构化表单；只提交明确勾选的字段。按来源列组核对分摊合计，保留原值、理由及操作者时间，不平均分摊，也不把合计复制给每期。
3. 根据整份文件摘要限制提交，跨页阻断同样生效。未保存修正时禁止翻页或提交。提交重新核对原文件；请求结果不确定时先重新读取会话，避免盲目重试。
4. 后端读取接口同时返回原始解析值和重新校验后的有效值；已提交会话可恢复导入结果。成功导入后刷新业务列表。
5. 与原有财务公式修复整合：预检规范化工作簿保留原文件销售/采购税率的单元格格式；多期流水写入后在同一事务内重算余额，避免将普通数值 0.5 误读为 50%，或保留写入前余额。

主要新增文件：`src/components/LegacyImportPreview.tsx`、`src/lib/legacyImportPreview.ts`、`tests/legacy-import-preview.test.ts`、`backend/tests/test_legacy_preview_display.py`。接口及整合修改见 `src/api.ts`、`src/components/OrdersScreen.tsx`、`backend/app/legacy_import_service.py`、`backend/app/routers/orders.py`。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| 前端测试 | 34 项通过 |
| 类型检查 / 构建 | `npm run lint`、`npm run build` 通过 |
| 完整后端集成测试 | 322 passed，2 failed，1 skipped |
| Git 检查 | 无未解决冲突，`git diff --check` 通过 |
| 测试环境 | Python 3.12.14；实际 MySQL 5.7.21-log；独立 `erp_ledger_test_*` 库；备份位于系统临时目录 |

两个失败与 PR #12 原始基线一致：`test_normal_financial_cases[N-05]` 和 `test_boundary_financial_cases[B-05]`。删除第 2 期付款后新增一期，测试期望 `[1, 3]`，实际 `[1, 2]`。本轮未修改断言规避失败；完整套件尚未全绿。详情见 `outputs/h3-preview-20260916/final-backend.log`。

浏览器使用合成 21 行台账验证了：上传、两页分页、其他页的阻断仍禁止提交、历史原值展示、错误分摊被拒绝、100 + 200 的正确修正、整批导入、已提交结果恢复、业务列表刷新。最后在原工作目录的整合版本重新走通恢复会话 → 人工修正 → 原文件核对 → 提交 → 重新读取 → 列表刷新。

最终独立数据库对账：21 个订单、21 条明细、2 期开票，开票合计 300.00 元，订单金额合计 4200.00 元。结果保存在 `outputs/h3-preview-20260916/browser-verification.json`。测试没有使用真实客户台账。

税率与余额专项用例核对：普通数值 0.5 与百分比格式 0.005 均按 0.5% 保留，订单金额 200.00，采购金额 20.10，开票 60 + 40 后待开票余额 100，收款 2 对应比例 2%。

复跑后端时，使用 `.temp/pr12-h3/backend/.venv/Scripts/python.exe`，设置独立 `MYSQL_DATABASE=erp_ledger_test_*` 和临时 `BACKUP_ROOT`，执行 `-m pytest backend/tests -q -p no:cacheprovider --basetemp=<独立临时目录>`。原 `backend/.venv` 的 Python 3.8 未替换。

## 下一步与未验收项

按 `docs/superpowers/plans/2026-09-15-legacy-ledger-history.md` 继续 H4：统一历史订单号搜索、包含历史负责人筛选、正式改号与框架整体交接、历史及完整期次导出，并验证别名不会使数量金额重复累计。

H5 与 B/C/D 阶段继续按原计划推进。真实旧表样本、两万行性能、MySQL 8.4、生产部署及恢复验收仍未完成；本轮 H3 合成数据验证不能替代这些验收。

原业务规则继续有效：框架 → 订单 → 子项目 → 明细；客户单位、最终用户和区域平台属于子项目；历史链按旧到新保留，允许甲 → 乙 → 甲；未知日期和金额分摊不能自动补全。
