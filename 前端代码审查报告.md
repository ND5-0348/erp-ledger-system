---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '29bb43ff-b4b1-474e-bb91-ada8e5b37a8d'
  PropagateID: '29bb43ff-b4b1-474e-bb91-ada8e5b37a8d'
  ReservedCode1: '547fa9a5-9aac-4e79-9c5d-6161f7597a99'
  ReservedCode2: '547fa9a5-9aac-4e79-9c5d-6161f7597a99'
---

# ERP 台账系统前端代码审查报告

> 审查日期：2026-09-03  
> 审查范围：`src/` 全部源码（18 个文件）+ 测试目录统计  
> 审查方式：逐行通读，只读研究，未修改任何文件  
> 技术栈：React 19 + Vite + Tailwind 4 前端 / FastAPI + MySQL 后端  

---

## 严重度说明

| 等级 | 含义 |
|------|------|
| **P1 阻断** | 会导致数据丢失、安全漏洞或功能不可用，上线前必须修复 |
| **P2 重要** | 功能缺陷或设计风险，影响用户体验或数据完整性 |
| **P3 一般** | 体验问题或代码健壮性不足，建议修复 |
| **P4 建议** | 代码规范、可维护性改进建议 |

---

## 问题汇总统计

| 严重度 | 数量 |
|--------|------|
| P1 阻断 | 0 |
| P2 重要 | 5 |
| P3 一般 | 14 |
| P4 建议 | 8 |
| **合计** | **27** |

---

## 按文件分组的问题清单

### 1. src/App.tsx（767 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 1 | **P2** | L231-236 | **limit=500 硬编码且数据静默截断**。`loadBackendData()` 对 ledgers/orders/purchases/sales 四个端点均写死 `{ limit: 500 }`。当数据库记录超过 500 条时，只加载前 500 条，前端无任何分页 UI、无总量提示、无截断警告。用户误以为看到的是全部数据。批量编辑器也只能选择已加载的 500 条。 |
| 2 | **P2** | L372-375 | **handleAddLedger 本地新增不持久化**。`handleAddLedger` 只调用 `setLedgers((prev) => [newItem, ...prev])`，未调用任何后端 API。LedgerScreen 提供了完整的新增表单 UI，用户填写后数据仅存于 React 状态，刷新页面即丢失。`api` 对象中也没有 `createLedger` 方法。日志中写"本地新增项目台账"表明开发者知晓，但用户无感知。 |
| 3 | **P3** | L408-411 | **handleAddSales 为死代码**。函数已定义（本地新增销售合同），但未被传入任何组件（SalesScreen 未接收 `onAddSales` prop），永远不会被调用。 |
| 4 | **P3** | L359-370 | **addLog 仅本地状态**。`addLog` 只执行 `setLogs((prev) => [newLog, ...prev])`，操作日志不写入后端。页面刷新后，本地日志被 `api.logs()` 返回的后端日志覆盖，历史本地日志全部消失。 |
| 5 | **P3** | L273-283 | **api.me() 非 401 失败时静默登出**。`api.me()` 失败时（如 500 错误），`.catch()` 直接清除 token 但不设置 `loginError`，用户看到空白登录页无任何解释。只有 401 才会通过 `UNAUTHORIZED_EVENT` 设置"登录已过期"提示。 |
| 6 | **P3** | L231-232 | **health 检查阻塞数据加载**。`api.health()` 与四个数据端点在同一个 `Promise.all` 中。若 health 端点异常但数据端点正常，整个数据加载失败。health 检查应独立于业务数据加载。 |
| 7 | **P4** | L654-656 | **numericQuantity 正则过于简单**。`/[\d.]+/` 从字符串中提取数字，对 "10.5箱（含包装）" 会提取 "10.5"，但对 "约10台" 会提取 "10"，对 "3套/盒" 会提取 "3"。边缘场景可能丢精度。 |
| 8 | **P4** | L182-188 | **mapBackup 丢失时间精度**。`backupTime: dateOnly(item.backup_time)` 只取日期部分（前 10 字符），但 SystemScreen 表头显示"备份时间"而非"备份日期"，用户无法看到具体备份时刻。与 `mapLog` 使用 `formatDatabaseUtcTime` 保留完整时间不一致。 |

### 2. src/api.ts（579 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 9 | **P3** | L326-344 | **Token 存 localStorage 存在 XSS 风险**。authToken 存于 `window.localStorage`，任何 XSS 攻击可直接读取 token。行业标准做法之一是 HttpOnly Cookie，或至少配合 CSP 策略降低风险。当前代码无任何 CSP 配置。 |
| 10 | **P3** | L574-575 | **logs/backups limit=100 硬编码**。`api.logs()` 写死 `?limit=100`，`api.backups()` 同样写死。SystemScreen 前端分页基于截断后的 100 条数据，若后端超过 100 条记录，分页显示的总页数是错误的。 |
| 11 | **P4** | L450 | **api.importExcel 死代码**。`importExcel` 方法定义了但从未被调用（App.tsx 使用的是 `importOrdersExcel`）。 |
| 12 | **P4** | L296-304 | **BackendAuthUser 缺少 department_scope 类型安全**。`department_scope: string[]` 类型声明为 `string[]`，但 `normalizeUser` 中未做防御性校验（若后端返回 null 会导致 `.includes()` 报错）。 |

### 3. src/components/LedgerScreen.tsx（896 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 13 | **P2** | L253 | **Mock 数据残留**。`warningCount: warningCount || 3` —— 当实际预警数为 0 时，显示为 3。注释明确写着 `// Default 3 warning to match screenshot exactly`，这是为了匹配设计稿截图而硬编码的假数据，上线后会误导用户。 |
| 14 | **P3** | L261, L185, L196, L134, L138 | **5 处 alert() 用户提示**。表单验证、导出失败、模板下载失败等均使用 `alert()` 弹窗，阻塞主线程且无法自定义样式。 |

### 4. src/components/OrdersScreen.tsx（1934 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 15 | **P3** | 全文 | **15 处 alert() 调用**。包括必填校验（L528/555）、操作成功提示（L622/318）、操作失败提示（L539/567/609/667/722/785/818）等，全部使用 `alert()`，用户体验较差。 |
| 16 | **P4** | L769 | **删除确认文案**。`window.confirm('确定删除这条采购信息吗？')` —— 该代码位于 OrdersScreen 中，确实是在删除订单详情中的采购信息，文案正确，但缺少订单编号上下文（如"确定删除订单 XXX 的采购信息吗？"）。 |

### 5. src/components/PurchasesScreen.tsx（1077 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 17 | — | L462, L932 | **due_payment_date 逻辑验证通过 ✓**。新增付款时 `nextPaymentPhase === 1 ? paymentForm.due_payment_date || null : null`，第一期允许填写到期付款日，第二期及以后强制为 null。编辑已有记录时允许修改。符合业务需求。 |
| 18 | **P3** | L220, L233 | **2 处 alert() 用户提示**。无数据提示和批量保存成功均使用 alert。 |

### 6. src/components/SalesScreen.tsx（869 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 19 | **P3** | L206, L219 | **2 处 alert() 用户提示**。与 PurchasesScreen 同类问题。 |

### 7. src/components/SystemScreen.tsx（1118 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 20 | **P3** | L416, L180, L178, L166, L164 | **5 处 alert() 用户提示**。备份创建成功/失败、恢复成功/失败、缓存刷新均使用 alert。 |
| 21 | **P4** | L1022, L1104 | **分页文本中英混合**。分页控件显示 `Page X / Y`，与全中文界面风格不一致。 |
| 22 | **P4** | L156 | **部门列表依赖 limit=500 数据源**。`availableDepartments` 从 `departments` prop 去重排序，但该数据源来自 App.tsx 的 `ledgers.map(...)`，而 ledgers 受 limit=500 截断。若有超过 500 个项目，某些部门可能不出现在用户创建表单的部门下拉中。 |

### 8. src/components/BatchOrderEditor.tsx（471 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 23 | **P3** | L216 | **硬编码列索引 slice(1, 23)**。`row.values.slice(1, 23)` 用魔法数字 1 和 23 界定可编辑列范围（B-W），未引用 `BASIC_INFORMATION_END_COLUMN` 常量或从 columns 定义动态计算。若后端调整列结构，此处静默失效。 |
| 24 | **P3** | L191-197 | **addRows 无上限控制**。`addRows(5)` 可无限调用，粘贴大块数据时 `pasteGrid` 的 `allowAddRows: true` 会自动扩行。若用户粘贴 10000 行 Excel 数据，会创建 10000 个 DOM 行（每行 20+ 列 = 200,000+ DOM 元素），可能导致浏览器卡顿甚至崩溃。无虚拟滚动保护。 |
| 25 | **P4** | L460 | **繁体字混入**。页脚文本"藍色表头可编辑"中"藍"为繁体字，应为简体"蓝"。 |

### 9. src/components/BatchPurchaseEditor.tsx（634 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 26 | **P2** | L132-136 | **financialColumns 硬编码列索引范围**。采购模式 `index >= 23 && index <= 66`、销售模式 `index >= 67 && index <= 90`。这些魔法数字完全依赖后端列定义的固定顺序，后端增删一列就会导致整个批量编辑器显示错误的列。应改为从后端返回的 column 元数据（如 `editable`、`key` 或分组标记）动态过滤。L148 的 `index <= 22` 同理。 |
| 27 | **P4** | L601 | **面板标题用词**。详情侧栏标题"A–W 基本信息详情"，使用"基"字正确，但侧栏从右侧滑入时宽度为 `max-w-2xl`，在小屏幕上可能占满全屏，缺少响应式适配。 |

### 10. src/components/BatchSalesEditor.tsx（8 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| — | — | — | **无问题**。该文件仅为 BatchPurchaseEditor 的包装组件，`mode="sales"` 透传，设计合理。 |

### 11. src/lib/batchOrderEditor.ts（358 行）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 28 | **P3** | L113 | **createBlankEditorRow 硬编码业务默认值**。`column.excel_column === 'A' ? '全额' : null` —— A 列（amount_type）默认值为"全额"，这是业务规则硬编码在通用工具函数中。若业务规则变更（如增加"差额"选项），需要修改工具函数。 |
| 29 | **P4** | L294-316 | **comparableEditorValue 正则不完整**。数字规范化正则 `/^([+-]?)(\d+)(?:\.(\d*))?$/` 不支持科学计数法（如 `1e5`）和前导小数点（如 `.5`）。虽在 ERP 场景中不太可能出现，但作为通用函数存在隐患。 |
| 30 | — | L334-357 | **normalizeEditorValue 精度保护设计 ✓**。对 number/percentage 类型，`Number(normalized)` 仅用于验证有效性，返回值为原始字符串文本（注释"Keep the original decimal text so 18-digit amounts do not lose precision"），避免 JS Number 精度损失。设计正确。 |

### 12. src/lib/permissions.ts

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 31 | **P4** | 全文 | **权限模型简单无层级**。`hasPermission` 仅做 `user.permissions.includes(permission)` 线性检查，无通配符、无角色层级继承。`system_admin` 不是超级权限（不会自动拥有其他权限），需后端在 `permissions` 数组中显式包含。当前设计可工作，但扩展性有限。 |

### 13. src/lib/dateTime.ts

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| — | — | — | **UTC 处理验证通过 ✓**。`formatDatabaseUtcTime` 对无时区后缀的字符串自动补 `Z`，使用 `Asia/Shanghai` 时区格式化为 `YYYY-MM-DD HH:mm:ss`。测试文件验证 `2026-08-04 10:48:29` → `2026-08-04 18:48:29`（UTC+8）。逻辑正确。 |

### 14. src/lib/orderAmounts.ts

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| 32 | **P3** | round() 函数 | **大数精度边界**。`Math.round((value + Number.EPSILON) * factor) / factor` 当 `value * factor > Number.MAX_SAFE_INTEGER`（约 9×10^15）时会丢失精度。对于金额超千万亿元（不太可能但非不可能）的极端场景有风险。常规 ERP 金额范围内无问题。 |

### 15. src/hooks/ 目录（3 个文件）

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| — | — | — | **三个 Hook 实现合理 ✓**。`useBatchEditorHistory`（undo 栈 MAX=100，cloneRows 浅拷贝+values 数组拷贝）、`useEditorCellSelection`（矩形拖拽选择+Ctrl+Shift 多选）、`useEditorColumnWidths`（拖拽调整列宽+双击重置）逻辑完整，无明显问题。 |

### 16. src/components/DashboardScreen.tsx

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| — | — | — | **无明显问题**。仪表盘数据来自 App.tsx 传入的 ledgers/orders/logs，纯展示组件，导航跳转通过 `onNavigate` 回调实现。 |

### 17. src/types.ts

| # | 严重度 | 位置 | 问题描述 |
|---|--------|------|----------|
| — | — | — | **无明显问题**。类型定义与后端接口对应，`optionalNumber` 和 `dateOnly` 辅助函数在 App.tsx 中正确定义和使用。 |

---

## 重点关注项验证结论

| 关注项 | 结论 | 依据 |
|--------|------|------|
| ① 登录 token + 401 处理闭环 | **通过 ✓** | `request()` 和 `ensureSuccessfulResponse()` 均处理 401：清空 token + dispatch `UNAUTHORIZED_EVENT`。App.tsx L286-294 监听该事件，调用 `clearSession()` 清空状态并显示登录页。逻辑闭环完整。 |
| ② 批量编辑器功能 | **通过（有 P3 风险）⚠️** | A-W 列边界正确（`BASIC_INFORMATION_END_COLUMN='W'`），Ctrl+C/V 粘贴、Ctrl+Z 撤销、只读列保护（`changeCell` 检查 `column.editable`）、脏数据识别（`editableEditorRowsMatch`）均实现。**风险**：①无行数上限，大块粘贴可能崩溃（#24）；②列索引硬编码（#23）。 |
| ③ 权限按钮覆盖 | **通过 ✓** | 10 个权限码覆盖 order/purchase/sales 的 entry/edit/delete + system_admin。各 Screen 接收对应的 can* 布尔值，条件渲染按钮（隐藏式，非禁用式）。App.tsx L457-466 权限计算正确。 |
| ④ 采购付款期次 due_payment_date | **通过 ✓** | 新增付款时 `nextPaymentPhase === 1` 才允许填写 due_payment_date，第二期及以后强制 null。编辑已有记录时允许修改。符合业务需求。 |
| ⑤ UTC 时间处理 | **通过 ✓** | `formatDatabaseUtcTime` 正确补 `Z` 并按 Asia/Shanghai 格式化。测试用例验证 UTC+8 转换正确。 |
| ⑥ 数字精度 | **通过（有 P3 注意）⚠️** | `normalizeEditorValue` 返回原始字符串文本避免 JS Number 精度损失（设计正确）。`orderAmounts.ts` 的 `round()` 函数对超大数有边界风险（#32），但常规范围内无问题。 |
| ⑦ 错误处理 | **部分通过 ⚠️** | 接口报错通过 `ApiError` throw → 组件 catch → `alert()` 提示，不会静默失败。但 **28 处 alert() 影响体验**（#14/15/18/19/20），且 `api.me()` 非 401 失败时无提示（#5）。 |

---

## 测试目录统计

### 前端测试（`tests/` 目录）

| 文件名 | 行数 | 覆盖模块 |
|--------|------|----------|
| batch-order-editor.test.ts | 275 | 批量编辑器核心逻辑（粘贴、复制、脏数据、payload 构建） |
| dashboard-metrics.test.ts | 261 | 仪表盘指标计算 |
| date-time.test.ts | 16 | UTC 时间格式化 |
| operation-log-display.test.ts | 239 | 操作日志详情展示 |
| order-amounts.test.ts | 22 | 金额/税额计算 |
| order-detail-tables.test.ts | 40 | 订单详情表格数据构建 |
| permissions.test.ts | 52 | 权限判断 |
| project-order-summary.test.ts | 137 | 项目订单汇总 |
| query-filter-model.test.ts | 261 | 查询过滤模型 |
| sales-detail-model.test.ts | 200 | 销售详情数据模型 |
| **合计** | **1,503 行** | **10 个测试文件** |

- **断言风格**：Node.js `assert` 模块（`assert.equal`/`assert.deepEqual`/`assert.ok`），非 Jest/Vitest
- **覆盖面**：核心 lib/ 工具函数覆盖较好（8/10 个 lib 模块有测试），hooks 和组件无单元测试
- **未覆盖**：App.tsx、各 Screen 组件、BatchOrderEditor.tsx、BatchPurchaseEditor.tsx、3 个 hooks 均无测试

### 后端测试（`backend/tests/` 目录）

| 文件名 | 行数 | 覆盖模块 |
|--------|------|----------|
| test_auth_permissions.py | 114 | 认证与权限 |
| test_auth_required_routes.py | 91 | 路由鉴权 |
| test_financial_integration.py | 2,132 | 财务集成（核心测试） |
| test_financial_validation.py | 84 | 财务校验 |
| test_ledger_excel_columns.py | 105 | Excel 列导入 |
| **合计** | **2,526 行** | **5 个测试文件** |

- **断言风格**：pytest（`assert` 语句）
- **覆盖面**：认证权限、财务计算、Excel 导入有测试覆盖
- **重点**：`test_financial_integration.py` 占 2,132 行，是后端最核心的测试文件

### 测试总计

| 维度 | 数值 |
|------|------|
| 前端测试文件 | 10 个 |
| 前端测试代码 | 1,503 行 |
| 后端测试文件 | 5 个 |
| 后端测试代码 | 2,526 行 |
| **测试代码总计** | **4,029 行** |

---

## 修复优先级建议

### 上线前建议修复（P2）

1. **#1 limit=500 数据截断** → 短期：加总量提示和"数据可能不完整"警告；长期：实现服务端分页
2. **#2 handleAddLedger 不持久化** → 要么接入后端 API，要么禁用新增按钮并在 UI 标注"暂未接入后端"
3. **#13 LedgerScreen mock 数据** → 删除 `|| 3`，显示真实预警数
4. **#26 financialColumns 硬编码索引** → 从后端 column 元数据动态计算列范围
5. **#10 logs/backups limit=100** → 与 #1 同类问题，至少显示总量

### 建议尽快修复（P3）

- 全局 `alert()` 替换为 Toast 通知组件（28 处，#14/15/18/19/20）
- `api.me()` 非 401 失败时给出用户提示（#5）
- `health` 检查独立于数据加载（#6）
- BatchOrderEditor 添加行数上限保护（#24）
- Token 存储 XSS 防护（#9，配合 CSP 策略）

---

*报告结束。本次审查为只读研究，未对项目任何文件进行修改。*

> AI生成